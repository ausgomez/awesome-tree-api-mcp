

export class StateService {
  constructor() {
  }

  rollbackToById(id: number) {
    // find the state by id
    // if not found return not found
    // delete the current table of trees
    // set the current table as the found state

  }

  saveState() {
    // Grab the whole current list of nodes
    // save it into a new state record witha a new Id
  }
}
